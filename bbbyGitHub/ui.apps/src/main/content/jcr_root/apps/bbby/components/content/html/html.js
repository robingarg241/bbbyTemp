'use strict';

/**
 *  This is the main file for html component
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Html {
    constructor($inst) {
        log.trace('Initializing Html')
    }
}

$('.cmp-html').each((index, elem) => {
    new Html($(elem))
})

*/
