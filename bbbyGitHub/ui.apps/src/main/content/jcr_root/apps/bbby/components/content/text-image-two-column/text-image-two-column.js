'use strict';

/**
 *  This is the main file for text-image-two-column
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class TextImageTwoColumn {
    constructor($inst) {
        log.trace('Initializing TextImageTwoColumn')
    }
}

$('.cmp-ti2c').each((index, elem) => {
    new TextImageTwoColumn($(elem))
})

*/
