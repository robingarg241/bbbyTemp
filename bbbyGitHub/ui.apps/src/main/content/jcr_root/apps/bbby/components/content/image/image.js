'use strict';

/**
 *  This is the main file for <%= jsModuleName %>
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Image {
    constructor($inst) {
        log.trace('Initializing Image')
    }
}

$('.cmp-image').each((index, elem) => {
    new Image($(elem))
})

*/
