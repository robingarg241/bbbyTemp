'use strict';

/**
 *  This is the main file for background-image
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class BackgroundImage {
    constructor($inst) {
        log.trace('Initializing BackgroundImage')
    }
}

$('.cmp-background-image').each((index, elem) => {
    new BackgroundImage($(elem))
})

*/
