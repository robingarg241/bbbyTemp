'use strict';

/**
 *  This is the main file for Pdm Metadata
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class PdmMetadata {
    constructor($inst) {
        log.trace('Initializing PdmMetadata')
    }
}

$('.cmp-pdm-metadata').each((index, elem) => {
    new PdmMetadata($(elem))
})

*/
