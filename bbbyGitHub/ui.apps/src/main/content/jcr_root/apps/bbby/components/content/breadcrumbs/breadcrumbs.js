'use strict';

/**
 *  This is the main file for breadcrumbs
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Breadcrumbs {
    constructor($inst) {
        log.trace('Initializing Breadcrumbs')
    }
}

$('.component-breadcrumbs').each((index, elem) => {
    new Breadcrumbs($(elem))
})

*/
