'use strict';

/**
 *  This is the main file for anchor
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel';

class Anchor {
    constructor($inst) {
        log.trace('Initializing Anchor')
    }
}

$('.cmp-anchor').each((index, elem) => {
    new Anchor($(elem))
})

*/
