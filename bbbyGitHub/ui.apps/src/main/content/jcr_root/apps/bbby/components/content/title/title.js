'use strict';

/**
 *  This is the main file for Title
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Title {
    constructor($inst) {
        log.trace('Initializing Title')
    }
}

$('.cmp-title').each((index, elem) => {
    new Title($(elem))
})

*/
