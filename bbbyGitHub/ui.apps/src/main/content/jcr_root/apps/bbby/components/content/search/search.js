'use strict';

/**
 *  This is the main file for search
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Search {
    constructor($inst) {
        log.trace('Initializing Search')
    }
}

$('.cmp-search').each((index, elem) => {
    new Search($(elem))
})

*/
