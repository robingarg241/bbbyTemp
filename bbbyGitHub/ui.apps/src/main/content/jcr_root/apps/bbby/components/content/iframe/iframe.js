'use strict';

/**
 *  This is the main file for Iframe
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Iframe {
    constructor($inst) {
        log.trace('Initializing Iframe')
    }
}

$('.cmp-iframe').each((index, elem) => {
    new Iframe($(elem))
})

*/
