'use strict';

/**
 *  This is the main file for hero-banner
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class HeroBanner {
    constructor($inst) {
        log.trace('Initializing HeroBanner')
    }
}

$('.cmp-hero-banner').each((index, elem) => {
    new HeroBanner($(elem))
})

*/
