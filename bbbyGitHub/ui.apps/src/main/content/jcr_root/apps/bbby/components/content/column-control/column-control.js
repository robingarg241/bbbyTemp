'use strict';

/**
 *  This is the main file for Column Control
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class ColumnControl {
    constructor($inst) {
        log.trace('Initializing ColumnControl')
    }
}

$('.cmp-column-control').each((index, elem) => {
    new ColumnControl($(elem))
})

*/
