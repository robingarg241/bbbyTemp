'use strict';

/**
 *  This is the main file for article
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Article {
    constructor($inst) {
        log.trace('Initializing Article')
    }
}

$('.cmp-article).each((index, elem) => {
    new Article($(elem))
})

*/
