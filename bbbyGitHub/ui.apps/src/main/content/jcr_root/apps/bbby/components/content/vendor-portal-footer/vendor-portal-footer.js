'use strict';

/**
*  This is the main file for vendor-portal-footer
*/

/* STARTER CODE

var $ = require('jquery');
var log = require('loglevel');

// module.exports = [publish public functions here];

$('.component-vendor-portal-footer').each(function() {
    init($(this))
})

function init($inst) {
    log.trace('Initializing VendorPortalFooter');
}

*/
