'use strict';

/**
 *  This is the main file for cta
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Cta {
    constructor($inst) {
        log.trace('Initializing Cta')
    }
}

$('.cmp-cta').each((index, elem) => {
    new Cta($(elem))
})

*/
