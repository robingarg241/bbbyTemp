'use strict';

/**
 *  This is the main file for background
 */

/*  STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class Background {
    constructor($inst) {
        log.trace('Initializing Background')
    }
}

$('.cmp-background').each((index, elem) => {
    new Background($(elem))
})

*/
