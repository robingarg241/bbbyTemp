'use strict';

/**
 *  This is the main file for <%= prettyName %>
 */

/* STARTER CODE

import $ from 'jquery'
import log from 'loglevel'

class <%= jsObjectName %> {
    constructor($inst) {
        log.trace('Initializing <%= jsObjectName %>')
    }
}

$('.<%= className %>').each((index, elem) => {
    new <%= jsObjectName %>($(elem))
})

*/
